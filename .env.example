# Database connection string for PostgreSQL
# Format: postgresql://username:password@host:port/database?sslmode=disable
DB_CONNECT_STRING=postgresql://user:password@localhost:5432/thepaper?sslmode=disable

# Portfolio URL (used for generating unsubscribe links)
# In production, set this to your actual domain
PORTFOLIO_URL=http://localhost:4040
